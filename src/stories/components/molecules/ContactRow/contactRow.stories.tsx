import React from 'react'

import ContactRow from './ContactRow'

export default {
  title: 'Components/Molecules/ContactRow',
  component: ContactRow,
}

export const Default = (): React.ReactElement => {
  return <ContactRow />
}
